name: 'Login Linux'
on:
  push:
    paths:
      - action.yml
      - .github/workflows/unittest.yml

jobs:
  unittests:
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        os: [ubuntu-latest]   
    steps:
      - uses: gridai-actions/.github/workflows/unittest.yml@main
        with:
          os: ${{ matrix.os }}
